# Profile manifest. Flat YAML subset, same reader as .ai/config.yaml.
name: ruby
description: Ruby projects (rubocop, rspec or minitest, all through bundle exec).
detect: [Gemfile]
scope: [changed, map, explain]
