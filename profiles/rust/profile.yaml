# Profile manifest. Flat YAML subset, same reader as .ai/config.yaml.
name: rust
description: Rust projects (cargo fmt, cargo clippy, cargo test from PATH).
detect: [Cargo.toml]
scope: [changed, map]
