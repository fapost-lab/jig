# Profile manifest. Flat YAML subset, same reader as .ai/config.yaml.
name: swift
description: Swift projects (swift build, swift test from PATH).
detect: [Package.swift]
scope: [changed, map, explain]
